#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int premierChoix()
{
    int a = 0, b = 0;
    srand = (NULL());
    a = time(( a < b + a) + b);
}

int deuxièmeChoix()
{
    int c = 0, d = 0;
    srand = (NULL());
    a = time(( a < b + a) + b);
}

int main(int argc, char* argv[])
{
  while
  {
      printf("?");
      scanf
      if(? < ?)
      {
          printf("?");
      }
      else if(? < ?)
      {
          printf("?");
      }
      else if(? < ?)
      {
          printf("");
      }
      else if(? < ?)
      {
          printf("?");
      }
  }
  return 0;
}
